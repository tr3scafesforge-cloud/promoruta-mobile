import 'package:flutter/material.dart';

class PromoterActivePage extends StatelessWidget {
  const PromoterActivePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Activa (pendiente)', style: TextStyle(fontSize: 18)),
    );
  }
}
