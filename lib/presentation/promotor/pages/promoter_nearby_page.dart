import 'package:flutter/material.dart';

class PromoterNearbyPage extends StatelessWidget {
  const PromoterNearbyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('En tu zona (pendiente)', style: TextStyle(fontSize: 18)),
    );
  }
}